# Skills — API Analyst

| Skill | Kedy |
|---|---|
| `buddy:persona-backend` | Pri analýze API designu, REST best practices, status code semantiky. |
| `buddy:persona-analyzer` | Pri reverse-engineeringu schém z PDF a porovnávaní s database views. |
| `buddy:persona-scribe` | Pri písaní `endpoints.md` — jasný technický markdown. |
| `buddy:generator-spec` | NIE — túto rolu rieši Domain Modeller, nie API analyst. |

## Custom utility

- `pdftotext -layout -f X -l Y docs/ca-service-management-17-4.pdf -` — primárny
  nástroj na extrakciu po blokoch.
- `npx tsc --noEmit --strict schemas/*.ts` — validácia TypeScript schém pred
  ukončením.

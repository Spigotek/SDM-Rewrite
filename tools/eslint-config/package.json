{
  "name": "@sdm/eslint-config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./index.js",
  "scripts": {
    "typecheck": "echo \"(no ts here)\" && exit 0",
    "lint": "echo \"(self)\" && exit 0",
    "test": "echo \"(no tests yet)\" && exit 0",
    "build": "echo \"(config-only package — no build)\" && exit 0",
    "clean": "rimraf .turbo"
  }
}

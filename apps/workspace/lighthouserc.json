{
  "_comment_scope": "Workspace LHCI config. Per performance.md §1 workspace pages are audited on DESKTOP form factor (Anna persona, 2-monitor workstation). H.0 wires React Router so `/queue` is reachable as a routed placeholder; timing thresholds graduate to error per performance.md §2 workspace `/queue` desktop (TTI ≤ 2.5 s, LCP ≤ 2.0 s, CLS ≤ 0.05, score ≥ 0.85). Other routes stay listed under `_url_todo_phase_h` and graduate as feature chunks (H.7+) ship real content.",
  "ci": {
    "collect": {
      "staticDistDir": "./dist",
      "url": ["http://localhost/index.html"],
      "_url_todo_phase_h": [
        "http://localhost/queue",
        "http://localhost/tickets/INC-1",
        "http://localhost/changes",
        "http://localhost/changes/calendar",
        "http://localhost/changes/CHG-1",
        "http://localhost/cmdb",
        "http://localhost/cmdb/ci/CI-1",
        "http://localhost/kb/editor"
      ],
      "numberOfRuns": 3,
      "settings": {
        "preset": "desktop",
        "chromeFlags": "--no-sandbox --headless=new --disable-gpu",
        "onlyCategories": ["performance", "accessibility", "best-practices"]
      }
    },
    "assert": {
      "_comment_h0_graduation": "H.0 graduates timing thresholds from warn to error: workspace `/queue` is now reachable via React Router redirect from `/`, so the same critical-path code as future feature routes runs. Numeric thresholds match performance.md §2 workspace `/queue` desktop. CLS/a11y/best-practices stay structural error gates.",
      "assertions": {
        "cumulative-layout-shift": ["error", { "maxNumericValue": 0.05 }],
        "categories:accessibility": ["error", { "minScore": 0.9 }],
        "categories:best-practices": ["error", { "minScore": 0.9 }],
        "categories:performance": ["error", { "minScore": 0.85 }],
        "interactive": ["error", { "maxNumericValue": 2500 }],
        "largest-contentful-paint": ["error", { "maxNumericValue": 2000 }],
        "total-blocking-time": ["error", { "maxNumericValue": 300 }]
      }
    },
    "upload": { "target": "temporary-public-storage" }
  }
}

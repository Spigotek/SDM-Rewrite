{
  "name": "@sdm/pm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "bin": {
    "sdm-pm": "./src/cli.ts"
  },
  "scripts": {
    "dev": "node --import tsx/esm src/cli.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "echo \"(no tests yet)\" && exit 0",
    "build": "echo \"(stub — tsup bundle in chunk 2)\" && exit 0",
    "clean": "rimraf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "tsx": "4.19.2"
  }
}
